/**
 * @format
 */

import 'react-native';
import renderer from 'react-test-renderer';
import BottomSheet from '../..';

describe('When: I press the button', () => {
  it('Then: The component appears from bottom to top', () => {
    expect(true).toBe(true);
  });
});
