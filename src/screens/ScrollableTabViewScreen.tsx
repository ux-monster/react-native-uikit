import React from 'react';
import {Text, View} from 'react-native';

interface Props {}

const ScrollableTabViewScreen = ({}: Props) => {
  return (
    <View>
      <Text>ScrollableTabViewScreen</Text>
    </View>
  );
};

export default ScrollableTabViewScreen;
