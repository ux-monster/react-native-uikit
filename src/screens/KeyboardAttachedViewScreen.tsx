import React from 'react';
import {Text, View} from 'react-native';

interface Props {}

const KeyboardAttachedViewScreen = ({}: Props) => {
  return (
    <View>
      <Text>KeyboardAttachedViewScreen</Text>
    </View>
  );
};

export default KeyboardAttachedViewScreen;
